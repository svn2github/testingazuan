<project name="${project.name}" default="makeDist" basedir=".">
  <!-- ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ -->
  <!--   DEFINIZIONE FILE DI PROPERTY						                                      -->
  <!-- ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ --> 
  <property file="build.properties"/>	
  <!-- ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ -->
  <!--   DEFINIZIONE CLASSPATH						                                            -->
  <!-- ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++ --> 
	<path id="web.classpath">
    		<pathelement path="${classpath}"/>
    		<!-- LIBRERIE SPECIFICHE APPLICATIVO  -->
    		<fileset dir="${web.lib}">
          		<include name="**/*.jar"/>
    		</fileset>
    		<!-- LIBRERIE ESTERNE DEL WEB CONTAINER necessarie per la compilazione -->
    		<!-- ma che non devono essere comprese nel war                         -->
    		<fileset dir="${runtime.lib}">
          		<include name="**/*.jar"/>
    		</fileset>
    		<!-- LIBRERIE DIPENDENZA SBI.UTILS necessarie per la compilazione      -->
    		<!-- che non devono essere comprese nel war                            -->
    		<fileset dir="${sbi.utils.lib}">
          		<include name="**/*.jar"/>
    		</fileset>  		
 	</path>

  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <!--   PULIZIA AMBIENTE						                                                -->
  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++--> 
  <target name="clean" >
    <!-- Delete the ${build} and ${dist} directory trees -->
    <delete dir="${build}"/>
    <delete dir="${dist}/${webapp_name}" />
  </target>	   	
  <target name="init" depends="clean">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
    <mkdir dir="${build}/${webapp_name}/WEB-INF/classes"/>
  </target>

  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <!--   SCARICO DEI SORGENTI AGGIORNATI DA CVS							                        -->
  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <target name="svn.checkout" depends="init"
      description="Sincronizza l'applicazione con il repository">
     <exec dir="${project.dir}" executable="svn" >
      <arg line="checkout ${svn.base.location}/${project.name} ."/>
    </exec>	 
  </target>

  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <!--   AGGIORNAMENTO JAR SBI.UTILS							                                  -->
  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <target name="sbi.utils.update" depends="svn.checkout"
      description="Updates sbi.utils library from distribution folder (THIS JAR MUST BE EXISTING)">
    <!-- deletes any previuos sbi.utils -->
    <delete>
      <fileset dir="${web.dir}/WEB-INF/lib" includes="**/sbi.utils*.jar"/>
    </delete>
    <!-- copies the newly created jar file -->
    <copy file="${sbiutils.dir}/dist/SpagoBIUtils/sbi.utils-${project.version}.jar" 
        todir="${web.dir}/WEB-INF/lib" overwrite="true" />
  </target>

  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <!--   AGGIORNAMENTO JAR SPAGOBI.SERVICE.CLIENT							                      -->
  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <target name="sbi.service.client.update" depends="svn.checkout"
      description="Updates spagobi.service.client library from distribution folder (THIS JAR MUST BE EXISTING)">
    <!-- deletes any previuos sbi.utils -->
    <delete>
      <fileset dir="${web.dir}/WEB-INF/lib" includes="**/spagobi.service.client*.jar,**/spagobi-service-client*.jar"/>
    </delete>
    <!-- copies the newly created jar file -->
    <copy file="${spagobi.service.client.dir}/dist/SpagoBI/spagobi.service.client-${project.version}.jar" 
        todir="${web.dir}/WEB-INF/lib" overwrite="true" />
  </target>

  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <!--   COMPILAZIONE							                                                  -->
  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <target name="compile" depends="sbi.utils.update,sbi.service.client.update" description="compile the source" >
    <!-- Compile the java code from ${src} into ${build}/${webapp_name}/WEB-INF/classes -->
    <javac srcdir="${src}" destdir="${build}/${webapp_name}/WEB-INF/classes" debug="on">
      <classpath refid="web.classpath"/>
    </javac> 
  </target>

  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->
  <!--   COPIA DELLE JSP,CSS,...                                                    -->
  <!--++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++-->	  	  
  <target name="copy.artifacts" depends="compile">
    <copy todir="${build}/${webapp_name}">
      <fileset dir="${web.dir}">
        <exclude name="**/*.svn"/>
      </fileset>
    </copy>
    <copy todir="${build}/${webapp_name}/WEB-INF/classes" >
      <fileset dir="${src}">
        <exclude name="**/*.java"/>
        <exclude name="**/*.svn"/>
      </fileset>
    </copy>
  </target>

	<target name="create.war" depends="copy.artifacts" >
	      <mkdir dir="${dist}/${webapp_name}"/>
	      <delete file="${dist}/${webapp_name}/${war_name}" />
	      <war warfile="${dist}/${webapp_name}/${war_name}" 
	           webxml="${build}/${webapp_name}/WEB-INF/web.xml" 
	           basedir="${build}/${webapp_name}" />
	</target>

  <target name="javadoc">    
    <javadoc packagenames="*"
      sourcepath="${src}"
      defaultexcludes="yes"
      destdir="${dist}/${webapp_name}/javadoc"
      author="true"
      version="true"
      use="true">
      <classpath refid="web.classpath"/>
    </javadoc>    
  </target>

  <target name="makeDist" >    
    <antcall target="create.war" />
    <antcall target="javadoc" />
  </target>

</project>
