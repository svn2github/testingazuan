<ivysettings>
  <resolvers>
    <filesystem name="local">
      <ivy pattern="${ivy.repo.local.dir}/[organisation]/[module]/ivys/ivy-[revision].xml" />
      <artifact pattern="${ivy.repo.local.dir}/[organisation]/[module]/[type]s/[artifact]-[revision].[ext]" />
    </filesystem>
  </resolvers>
</ivysettings>


