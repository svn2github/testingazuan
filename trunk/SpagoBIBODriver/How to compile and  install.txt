========================= COMPILE BO DRIVER ===========================In order to compile the BOEngine you need Eclipse 3.2 with WTP - import the project into eclipse- solve the classpath problems- compile the project- export the compiled source as a jar file (bodriver.jar)========================= INSTALL BO DRIVER ===========================- copy the bodriver.jar into the WEB-INF/lib folder of yours   SpagoBI web application folder