L'installazione di SpagoBIDemo sul tuo PC e' completata.

Ora puoi far partire il database e Exo-Tomcat.
Lancia nell'ordine i seguenti file:

- start.bat per Windows o start.sh per Unix all'interno della cartella 
sbidata/databases di Exo-Tomcat (Controllare i permessi di esecuzione);
- exo-run.bat per Windows o exo-run.sh per Unix all'interno della cartella 
bin di Exo-Tomcat;
- per i booklets (fascicoli), chiudi tutte le applicazioni Open Office e 
dalla cartella {OpenOffice-2.0.x-Home}/program esegui il seguente comando:
soffice -quickstart -accept="socket,host=localhost,port=9000;urp;StarOffice.ServiceManager"

Collegati, con il tuo browser (Explorer/Firefox) all'indirizzo 
http://localhost:8080/sbiportal